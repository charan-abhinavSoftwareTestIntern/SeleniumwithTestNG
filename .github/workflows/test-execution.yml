name: Scheduled Test Execution

on:
  schedule:
    - cron: "0 2 * * *"  # Runs daily at 2 AM UTC

  workflow_dispatch:  # Allows manual execution

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install Chrome & ChromeDriver
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          CHROME_VERSION=$(google-chrome --version | awk '{print $3}' | cut -d '.' -f 1)
          sudo apt-get install -y chromium-chromedriver
          echo "CHROME_DRIVER_PATH=/usr/lib/chromium-browser/chromedriver" >> $GITHUB_ENV

      - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Run Tests with Maven
        run: |
          mvn clean test -Dwebdriver.chrome.driver=$CHROME_DRIVER_PATH
        continue-on-error: false  # Fails workflow if tests fail

      - name: Generate Allure Report
        run: mvn allure:report

      - name: Upload Allure Results
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: target/allure-results

      - name: Upload Allure Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/allure-report

      - name: Send Email with Test Report
        run: |
          echo "Sending Email with Test Results"
          # Add your email sending script here

      - name: Send Google Chat Notification
        run: |
          echo "Sending Google Chat Notification"
          # Add your Google Chat webhook script here
